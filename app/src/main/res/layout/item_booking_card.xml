<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Service name + share button row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvCardService"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Service"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#3E2723"/>

            <ImageButton
                android:id="@+id/btnCardShare"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_menu_share"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Share booking"/>
        </LinearLayout>

        <!-- Package + price -->
        <TextView
            android:id="@+id/tvCardPackage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Package · ₹000"
            android:textSize="13sp"
            android:textColor="#3E2723"
            android:layout_marginTop="6dp"/>

        <!-- Date and time -->
        <TextView
            android:id="@+id/tvCardDateTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📅 Date  🕐 Time"
            android:textSize="13sp"
            android:textColor="#666666"
            android:layout_marginTop="6dp"/>

        <!-- Address -->
        <TextView
            android:id="@+id/tvCardAddress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📍 Address"
            android:textSize="13sp"
            android:textColor="#666666"
            android:layout_marginTop="4dp"/>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#F0E6DE"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="4dp"/>

        <!-- Action buttons row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Reschedule button -->
            <Button
                android:id="@+id/btnCardReschedule"
                android:layout_width="0dp"
                android:layout_height="36dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="Reschedule"
                android:textSize="12sp"
                android:textColor="#3E2723"
                android:backgroundTint="#F5EDE8"
                style="?attr/materialButtonOutlinedStyle"
                app:strokeColor="#3E2723"
                app:strokeWidth="1dp"
                app:cornerRadius="8dp"/>

            <!-- Cancel booking button -->
            <Button
                android:id="@+id/btnCardCancel"
                android:layout_width="0dp"
                android:layout_height="36dp"
                android:layout_weight="1"
                android:text="Cancel Booking"
                android:textSize="12sp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#D32F2F"
                app:cornerRadius="8dp"/>

        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>